package com.example.docparser.config;

import org.springframework.context.annotation.Configuration;

/**
 * AWS configuration removed for demo version.
 * This application now uses local filesystem instead of S3.
 */
@Configuration
public class AwsConfig {
    // AWS configuration removed for demo purposes
    // The application now uses FileService for local file operations
}
